import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

const textStyle = TextStyle(
  color: Colors.grey,
);

const Color insideBorderColorWrong = Color.fromRGBO(0, 200, 0, 0.5);

const Color insideBorderColorTrue = Color.fromRGBO(200, 0, 0, 0.6);

const Color borderColorWrong = Color.fromRGBO(0, 200, 0, 0.4);

const Color borderColorTrue = Color.fromRGBO(200, 0, 0, 0.6);

const Color trueAnswer = Colors.green;

const Color wrongAnswer = Colors.red;

const standartColor = Colors.grey;
